"""Universal Data Layer (§30): databases as agent data sources.

One protocol, many engines, read-only by layered guardrails — never by trust.
These are sources agents query; the app's internal store stays SQLite/Postgres
(§27.2 anti-matrix rule).
"""
